import { AboutView } from 'src/sections/about/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'About me',
};

export default function AboutPage() {
  return <AboutView />;
}
